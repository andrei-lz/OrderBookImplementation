#include "tests.hpp"

int main()
{
    // Some tests / display of the above class in action.
    // Hard-coded some values for input to the class, extracted them back out, assert and/or print the results.

    runTests();
}
